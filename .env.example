# Application
NODE_ENV=development
PORT=3000

# Database (Phase 1: SQLite)
# For SQLite (default, Phase 1):
DATABASE_URL=file:./prisma/data/cocoa_canvas.db

# For PostgreSQL (uncomment to use, Phase 2+):
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/cocoa_canvas

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here

# Automatic Setup (Phase 1)
# If ADMIN_EMAIL and ADMIN_PASSWORD are provided, the application will automatically
# create the initial admin user on first run, skipping the web-based setup wizard.
# This is useful for automated deployments (Docker, CI/CD, etc.)
#
# Example:
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=SecurePassword123!
# ADMIN_NAME=Administrator
# AUTO_SETUP_ENABLED=true
#
# All are optional. If not provided, users must complete the setup wizard via the web UI.
# ADMIN_EMAIL=
# ADMIN_PASSWORD=
# ADMIN_NAME=Admin
# AUTO_SETUP_ENABLED=true

# Security
# Generate a secure secret: openssl rand -base64 32
# NEXTAUTH_SECRET=<generated-value>

# Optional: OAuth Configuration (Phase 2+)
# GITHUB_ID=
# GITHUB_SECRET=
# GOOGLE_ID=
# GOOGLE_SECRET=

# Optional: Session configuration
SESSION_TIMEOUT=1800

# Logging
LOG_LEVEL=info

# Feature flags
ENABLE_OAUTH=false
ENABLE_MFA=false
ENABLE_LDAP=false
